# Ticket Classifier (Customer Support) — ML + API + Docker

End-to-end demo: data -> training -> evaluation -> deployable FastAPI service.

## 1) Setup
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
pip install -r requirements.txt